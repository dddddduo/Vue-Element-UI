import { LYD } from '../config/LYD'

// 例如：
// 如果post接口是json格式，需在最后加上'json'
// export const posts1 = params => MTHR.POST("/def/post/jiekou", params, 'json')
// export const posts = params => MTHR.POST("/def/post/jiekou", params)
// 如果get接口无参数，不需要传params
// export const gets = params => MTHR.GET("/def/get/jiekou", params)